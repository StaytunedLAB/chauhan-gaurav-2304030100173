function sum(a, b) {
  return a + b;
}

console.log(`1 + 2 = ${sum(1, 2)}.`); 

//acesseing characters in a string

let str = `Hello`;

// the first character
console.log( str[0] ); 
console.log( str.at(0) ); 

// the last character
console.log( str[str.length - 1] ); 
console.log( str.at(-1) );